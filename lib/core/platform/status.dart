enum ProcessState {
  success,
  failure,
  error,
  uploading,
  delete,
  idle,
}

enum AuthStatus {
  notLoggedIn,
  notRegistered,
  loggedIn,
  registered,
  authenticating,
  registering,
  loggedOut,
}
